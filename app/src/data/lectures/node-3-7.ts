// 3.7 轨迹方程与探索性问题 讲义
const lecture3_7 = `
## 轨迹方程与探索性问题

### 一、轨迹方程：三种方法

**方法一：定义法（最高效）**

当动点满足的条件恰好匹配某圆锥曲线的几何定义时，直接套用标准方程。

**操作步骤**：
1. 分析动点满足的几何条件
2. 判断是否匹配椭圆/双曲线/抛物线的定义
3. 确定基本量 $a$、$b$、$c$、$p$
4. 根据焦点位置写出标准方程

**适用信号**：
- 条件中出现"到两定点距离之和为定值" → 椭圆
- 条件中出现"到两定点距离之差为定值" → 双曲线
- 条件中出现"到定点距离等于到定直线距离" → 抛物线

**方法二：相关点法（坐标转移法）**

当动点 $M(x,y)$ 随另一已知曲线上的点 $P(x_0, y_0)$ 运动时使用。

**操作步骤**：
1. 设 $M(x,y)$ 为所求轨迹上的动点，$P(x_0, y_0)$ 为已知曲线上对应的点
2. 根据题目的几何关系，建立 $M$ 与 $P$ 的坐标关系：
   $$\\begin{cases} x_0 = f(x, y) \\\\ y_0 = g(x, y) \\end{cases}$$
3. 将 $P(x_0, y_0)$ 代入已知曲线方程 $F(x_0, y_0) = 0$：
   $$F\\big(f(x,y), g(x,y)\\big) = 0$$
4. 化简，得轨迹方程

**典型应用**：
- $M$ 是线段 $AB$ 的中点，$A$ 在已知曲线上运动，$B$ 为定点
- $M$ 满足 $\\overrightarrow{PM} = \\lambda \\overrightarrow{PQ}$，其中 $P$ 在已知曲线上

**方法三：参数法**

引入参数表示动点 $M(x,y)$，消参得轨迹方程。

**操作步骤**：
1. 设动点 $M(x,y)$，选择合适的参数 $t$
2. 用 $t$ 表示 $x$ 和 $y$：$\\begin{cases} x = \\varphi(t) \\\\ y = \\psi(t) \\end{cases}$
3. 消去 $t$，得 $x$ 与 $y$ 的关系式
4. 确定轨迹范围（由参数 $t$ 的取值范围导出 $x,y$ 的取值范围）

**参数选择技巧**：
- 直线旋转 → 斜率 $k$ 为参数
- 点在曲线上运动 → 参数方程中的角 $\\theta$ 或 $t$ 为参数
- 圆上点 → 圆心角 $\\theta$ 为参数

**轨迹方程的第四步——范围检验（极易丢分）**：

写出轨迹方程后务必检验：
- 动点是否能到达方程所描述的曲线上的每一点
- 是否有特殊位置需要排除（如某些点不满足原始条件）
- 是否存在无解的情况（如 $\\Delta < 0$ 时对应点不存在）

### 二、轨迹方程方法选择指南

| 条件特征 | 推荐方法 | 示例 |
|---------|---------|------|
| 动点条件直接匹配定义 | **定义法** | $|MA|+|MB|=4$，$|AB|=2$ → 椭圆 |
| 动点依附于已知曲线上另一点 | **相关点法** | $M$ 是 $AB$ 中点，$A$ 在椭圆上 |
| 动点坐标可方便地用某参数表示 | **参数法** | 椭圆上点到定点连线中点轨迹 |
| 动点条件为复杂的等式关系 | **直接法**（设 $M(x,y)$ 代入条件化简） | $|MA|^2 - |MB|^2 = 4$ |

### 三、探索性 / 存在性问题

**解题总思路**：假设存在 → 正向推理 → 验证是否与已知条件矛盾。

**标准格式**：
1. **假设**：假设存在满足条件的几何对象（点、直线等）
2. **设参**：设出相关参数
3. **列式**：根据条件建立方程（组）
4. **求解**：解方程，得出参数值或参数满足的范围
5. **验证**：验证结果是否满足题目所有条件（如 $\\Delta > 0$、点在曲线内部等）
6. **结论**：若方程有解且验证通过 → "存在"，给出具体结果；若出现矛盾 → "不存在"

**常见探索性问题类型**：

**类型一：是否存在某定点 / 定直线**
- 设出定点坐标 $(x_0, y_0)$ 或定直线方程
- 利用"对该类型所有情形均成立"构造关于坐标的方程组
- 解方程 → 判断是否有解

**类型二：是否存在满足某几何条件的点**
- 设点坐标，代入条件
- 利用曲线方程消元，解出坐标
- 验证该点是否在曲线的定义域内

**类型三：是否存在某参数使得某性质成立**
- 设参数值，代入条件
- 得到关于参数的方程
- 求解 + 验证 $\\Delta > 0$ 等相关约束

### 四、答题规范与采分点

轨迹方程题的标准答题步骤：
1. **建立坐标系**（如题目未给）或声明使用已有坐标系
2. 设动点 $M(x,y)$
3. 将几何条件翻译为代数方程
4. 化简整理得轨迹方程
5. **说明轨迹范围**（去除非轨迹部分） ← 重要采分点

探索性问题的标准格式：
1. 明确写出"假设存在……"（不能省略，这是格式分）
2. 给出完整的推理过程
3. 写出明确结论："存在……" 或 "不存在……"（不能只列方程不写结论）

### 五、常见易错点

- 轨迹方程写完后**不求范围**——不是曲线上的所有点都满足原始条件，必须标注 $x$ 或 $y$ 的范围或排除点
- 相关点法中**坐标关系列反**——注意区分哪个点是已知曲线上的，哪个是所求轨迹上的
- 定义法直接写出标准方程后，**忘记确定焦点在哪个轴上**
- 探索性问题中，方程解出来了就写"存在"，但**没有验证 $\\Delta > 0$** 等约束条件
- 参数法**消参不彻底**——最终方程中残留参数 $t$，需要进一步消去
`;
export default lecture3_7;
