// 5.2 隐零点与虚设零点 讲义
const lecture5_2 = `
## 隐零点与虚设零点

### 一、什么是隐零点

在导数综合题中，经常遇到导函数 $f'(x)$ 的零点无法用初等方法显式解出的情况。例如：

$$f'(x) = e^x + \\ln x, \\quad f'(x) = e^x + x - 2$$

这类方程 $f'(x) = 0$ 的根虽然存在（由零点存在定理可以证明），但无法写出精确的解析表达式。这个无法显式表达但确实存在的零点，称为**隐零点**（或虚设零点）。

### 二、虚设零点法（「设而不求」）

**核心思想**：设导函数的零点为 $x_0$，利用 $f'(x_0) = 0$ 建立关系式，在求最值时整体代入消去 $x_0$ 或化简表达式，最终无需解出 $x_0$ 的具体值。

**标准步骤**：

1. **求导**：计算 $f'(x)$，分析 $f'(x)$ 的单调性
2. **证明隐零点存在**：利用零点存在定理 + 导函数的单调性，证明 $f'(x)$ 有唯一零点 $x_0$，并估算 $x_0$ 所在区间
3. **虚设**：设 $f'(x_0) = 0$，得到 $x_0$ 满足的关系式（如 $e^{x_0} = \\frac{1}{x_0}$）
4. **代入化简**：将关系式代入 $f(x_0)$，消去指数或对数项，得到不含超越函数的最简表达式
5. **求范围**：利用 $x_0$ 所在区间，求出 $f(x_0)$ 的取值范围

### 三、经典示例

**例**：已知 $f(x) = x + e^x$，求 $g(x) = \\ln x + x - f(x)$ 的最小值。

> $g(x) = \\ln x + x - x - e^x = \\ln x - e^x$。
> $g'(x) = \\frac{1}{x} - e^x$，$g'(x)$ 在 $(0, +\\infty)$ 上严格递减（两项均递减）。
> $g'(\\\\frac{1}{2}) = 2 - \\sqrt{e} > 0$，$g'(1) = 1 - e < 0$，故存在唯一 $x_0 \\in (\\\\frac{1}{2}, 1)$ 使 $g'(x_0) = 0$。
> 由 $g'(x_0) = 0$ 得 $\\frac{1}{x_0} = e^{x_0}$，即 $e^{x_0} = \\frac{1}{x_0}$。
> $g(x_0) = \\ln x_0 - e^{x_0} = \\ln x_0 - \\frac{1}{x_0}$。
> 由 $x_0 \\in (\\\\frac{1}{2}, 1)$ 及 $h(x) = \\ln x - \\frac{1}{x}$ 的单调性，可进一步估计 $g(x_0)$ 的范围。

### 四、隐零点存在区间的估计

估计隐零点落在哪个区间，常用策略：

1. **猜根法**：尝试 $x = 1, \\frac{1}{e}, e$ 等特殊值计算 $f'(x)$ 的符号
2. **二分法**：在 $(a, b)$ 内逐步缩小区间，使两端函数值异号
3. **利用已知不等式**：如 $e^x \\ge x + 1$、$\\ln x \\le x - 1$ 等放缩后判断符号

### 五、隐零点代换的常用技巧

由 $f'(x_0) = 0$ 得到的关系式（通常是指数与对数、幂函数与对数的关系）是解题的关键。常见类型：

| $f'(x)$ 类型 | $f'(x_0) = 0$ 给出的关系 | 代入方向 |
|-------------|------------------------|---------|
| $e^x - \\frac{1}{x}$ | $e^{x_0} = \\frac{1}{x_0}$ | 用 $\\frac{1}{x_0}$ 替换 $e^{x_0}$ |
| $\\ln x + 1$ | $\\ln x_0 = -1$ | 直接解出 $x_0 = \\frac{1}{e}$（这不是隐零点！）|
| $e^x + \\ln x$ | $e^{x_0} = -\\ln x_0$ | 代入原函数时消去一项 |

> 当 $f'(x) = 0$ 可以直接解出 $x_0$ 时，就不是隐零点问题了——直接解出来用即可。
`;

export default lecture5_2;
