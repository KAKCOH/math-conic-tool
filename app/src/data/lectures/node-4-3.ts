// 4.3 导数与函数单调性 讲义
const lecture4_3 = `
## 导数与函数单调性

### 一、导数与单调性的关系

设函数 $f(x)$ 在区间 $I$ 上可导，则：

- $f'(x) > 0$（$x \\in I$）$\\Rightarrow$ $f(x)$ 在 $I$ 上**严格递增**
- $f'(x) < 0$（$x \\in I$）$\\Rightarrow$ $f(x)$ 在 $I$ 上**严格递减**
- $f'(x) \\ge 0$（$x \\in I$）且等号不构成区间 $\\Rightarrow$ $f(x)$ 在 $I$ 上**递增**

> **注意**：$f'(x) = 0$ 对单调性无直接结论（如 $f(x) = x^3$ 在 $x=0$ 处 $f'(0)=0$，但函数在 $\\mathbb{R}$ 上递增）。

### 二、求单调区间的标准步骤

1. 确定定义域（注意定义域是求单调区间的前提）
2. 求导 $f'(x)$
3. 解方程 $f'(x) = 0$（求驻点）和 $f'(x)$ 不存在的点
4. 用这些点将定义域分区
5. 在每个区间内判断 $f'(x)$ 的符号
6. 写出单调区间（用「递增」「递减」或 $\\nearrow$、$\\searrow$ 表示）

### 三、含参讨论单调性的策略

**情况 1：导函数为一次函数型**

$f'(x) = ax + b$ 型，按 $a > 0$、$a = 0$、$a < 0$ 分类讨论。

**情况 2：导函数为二次函数型**

$f'(x) = ax^2 + bx + c$ 型（$a \\neq 0$），按 $\\Delta$ 的符号分类：
- $\\Delta \\le 0$ 且 $a > 0$：$f'(x) \\ge 0$，$f$ 在定义域上递增
- $\\Delta \\le 0$ 且 $a < 0$：$f'(x) \\le 0$，$f$ 在定义域上递减
- $\\Delta > 0$：$f'(x) = 0$ 有两个不等实根，分区讨论

**情况 3：导函数含对数/指数**

通常需先分析导函数本身的单调性，再结合零点存在定理确定导函数的符号变化。

### 四、常见参数分类的「临界点」

对于 $f'(x) = \\frac{x-a}{g(x)}$ 型（$g(x) > 0$）：
- $a \\le 0$ 时，$x - a > 0$ 在 $(0, +\\infty)$ 上恒成立
- $a > 0$ 时，$x = a$ 是符号的分界点

临界值通常来自：
- 导函数零点的位置（与定义域端点比较）
- 导函数系数为零（降次）
- 判别式 $\\Delta = 0$

### 五、单调性证明不等式

利用单调性是证明不等式的基本方法之一：

**思路**：要证 $f(x) > g(x)$（$x \\in I$），构造 $F(x) = f(x) - g(x)$，证明 $F(x)$ 在 $I$ 上递增且 $F($左端点$) \\ge 0$，或递减且 $F($右端点$) \\le 0$。

> 经典结论：$e^x \\ge x + 1$（$x \\in \\mathbb{R}$），等号仅当 $x = 0$。
`;

export default lecture4_3;
