// 6.4 导数与函数零点 讲义
const lecture6_4 = `
## 导数与函数零点

### 一、零点问题的核心思路

导数是研究函数零点个数和分布的核心工具。完整的零点分析涉及：

1. 用导数确定函数的单调区间和极值
2. 计算各极值及端点/无穷远处的极限值
3. 利用**零点存在定理**（介值定理）确定每个单调区间内零点的存在性和唯一性

### 二、零点个数分析的标准流程

1. 求导 $f'(x)$，确定单调区间和极值
2. 将定义域按单调区间分段
3. 在每段内：计算区间端点的函数值（或极限），比较极值符号
4. 根据「单调 + 异号 → 唯一零点」原则，统计零点个数

**关键规则**：若 $f$ 在 $[a, b]$ 上**连续且严格单调**，且 $f(a)f(b) < 0$，则 $f$ 在 $(a, b)$ 内有**唯一**零点。

### 三、含参零点问题的分析框架

**典型问题**：已知 $f(x) = g(x) - a$，讨论 $f(x)$ 的零点个数与 $a$ 的关系。

这类问题的图像直觉：$f(x)$ 的零点 $\\iff$ 水平线 $y = a$ 与 $y = g(x)$ 图像的交点。

**分析步骤**：
1. 求 $g(x)$ 的单调区间和极值
2. 画出 $g(x)$ 的大致图像（只需关心极值和极限趋势）
3. 讨论水平线 $y = a$ 与图像的交点个数：
   - $a > g_{\\max}$ 或 $a < g_{\\min}$：0 个交点
   - $a$ 介于极值之间：根据单调区间个数确定交点数

**示例**（$f(x) = x^3 - 3x - a$）：
> $g(x) = x^3 - 3x$，$g'(x) = 3(x-1)(x+1)$。
> 极大值 $g(-1) = 2$，极小值 $g(1) = -2$。
> $|a| > 2$：1 零点；$|a| = 2$：2 零点；$|a| < 2$：3 零点。

### 四、隐零点在零点问题中的应用

当需要比较 $f(x)$ 在某点的符号、但该点无法直接计算时，隐零点方法发挥作用。

**思路**：若需要判断 $f(x_0)$ 的符号（$x_0$ 为 $f'(x)$ 的隐零点），利用 $f'(x_0) = 0$ 消去 $f(x_0)$ 中的超越表达式后再判断符号。

### 五、零点偏移与双零点关系

若 $f(x)$ 有两个零点 $x_1, x_2$，常见问题是证明关于 $x_1 + x_2$、$x_1 x_2$ 的不等式（即极值点偏移问题，见节点 5.3）。

处理策略：
1. 由 $f(x_1) = f(x_2) = 0$ 建立等式
2. 比值代换 $t = x_2/x_1$（或 $t = x_2 - x_1$）
3. 将结论转化为 $t$ 的单变量不等式

### 六、零点问题的书写要点

1. **零点存在性**：必须说明 $f$ 连续 + 两端函数值异号
2. **零点唯一性**：必须说明 $f$ 在该区间上严格单调
3. **参数讨论**：每个参数分段都要完整写出零点个数的结论
4. **开区间的极限**：当零点在 $(a, +\\infty)$ 上时，需计算 $\\lim_{x \\to +\\infty} f(x)$ 来与极值比较符号
`;

export default lecture6_4;
