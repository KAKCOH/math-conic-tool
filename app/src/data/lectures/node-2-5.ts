// 2.5 仿射变换 讲义
const lecture2_5 = `
## 仿射变换

仿射变换是一种"化椭圆为圆"的技巧。椭圆问题在代数上往往比圆复杂得多，但如果能把椭圆压成圆，在圆中解决问题，再把结果映射回椭圆，计算量可以大幅缩减。

### 一、什么是仿射变换

对椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$，作变换：

$$\\begin{cases} x' = \\dfrac{x}{a} \\\\ y' = \\dfrac{y}{b} \\end{cases}$$

椭圆变为单位圆：$x'^2 + y'^2 = 1$。

更一般地，对 $y$ 方向进行拉伸：$y' = \\frac{a}{b}y$（使 $a=b$ 的效果），椭圆变为半径为 $a$ 的圆。

![仿射变换：椭圆变为圆，对应点映射关系](/figures/affine_transform.png)

**几何直观**：仿射变换相当于将原来的 $xOy$ 平面沿 $y$ 轴方向均匀压缩（或拉伸），把椭圆"压圆"。

### 二、仿射变换保持什么、不保持什么

| 保持（不变） | 不保持（改变） |
|-------------|---------------|
| **平行性**：平行线仍为平行线 | **角度**：角度一般会变 |
| **线段比例**：共线三点的单比不变 | **长度**：非水平/竖直方向的长度改变 |
| **面积比**：面积比不变（面积本身按比例缩放） | **垂直关系**：垂直一般不再垂直 |
| **中点**：弦的中点仍是中点 | **圆的方程**：椭圆变圆后不再满足原方程 |
| **相切**：切点仍为切点 | **焦点**：焦点不是仿射不变量 |

> 最关键的三个保持：平行、比例、面积比。最需要注意的是角度和垂直不保持——所以在圆中用的垂径定理等不能直接搬回椭圆！

### 三、面积关系

椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ 的面积为 $\\pi ab$。

仿射变换 $x' = x/a$，$y' = y/b$ 将椭圆变为单位圆（面积 $\\pi$），变换的雅可比行列式：

$$\\frac{\\partial(x', y')}{\\partial(x, y)} = \\frac{1}{ab}$$

因此椭圆面积 $S_{\\text{椭圆}} = \\pi \\cdot ab = \\pi ab$。更一般地，变换前后的面积关系为：

$$S_{\\text{原}} = ab \\cdot S_{\\text{变换后}}$$

对于变换 $x'=x$，$y'=\\frac{a}{b}y$（将椭圆压成半径为 $a$ 的圆），面积关系为 $S_{\\text{原}} = \\frac{b}{a} \\cdot S_{\\text{变换后}}$。

### 四、解题步骤

椭圆问题 $\\to$ 仿射变换 $\\to$ 圆的问题 $\\to$ 解圆的问题 $\\to$ 反变换回椭圆

**具体流程**：

1. **实施变换**：$x' = x/a$，$y' = y/b$，椭圆 $\\to$ 单位圆
2. **转换条件**：将原题中的点坐标、直线方程按变换改写
3. **在圆中求解**：利用圆的性质（半径相等、圆心到弦的距离、圆幂定理等）解题
4. **反变换**：将结果从 $(x', y')$ 坐标映射回 $(x, y)$ 坐标

### 五、典型应用

#### 1. 弦中点问题

在圆中，弦的中点与圆心连线垂直于弦。利用这一性质，配合仿射不变性，可以快速求出椭圆弦中点满足的关系——这与点差法得到的 $k_{AB} \\cdot k_{OM} = -b^2/a^2$ 完全一致，但推导更直观。

#### 2. 面积最值

椭圆内接三角形/四边形的面积最值问题，仿射为圆内接图形的面积最值（圆内接正多边形面积最大），再乘以面积缩放因子 $ab$ 即可。

例如：椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ 内接三角形的最大面积为 $\\frac{3\\sqrt{3}}{4}ab$（圆内接等边三角形面积 $\\frac{3\\sqrt{3}}{4}$ 乘以 $ab$）。

#### 3. 线段比例关系

共线三点 $A$、$B$、$C$ 的**单比**（$\\frac{AB}{BC}$，带符号的方向比）在仿射变换下保持不变。因此，含三点共线的定比分点问题可以仿射到圆中处理。

| 问题类型 | 圆中的解法 | 反变换注意事项 |
|----------|-----------|---------------|
| 弦中点轨迹 | 圆心与中点连线垂直于弦 | $k_{AB} \\cdot k_{OM}$ 需乘以系数 |
| 内接多边形面积最值 | 正多边形面积最大 | 面积乘以 $ab$ |
| 共线三点的定比 | 利用圆的对称性 | 单比不变，直接回代 |
| 切线问题 | 圆心到切线距离等于半径 | 切点坐标乘系数反变换 |

### 六、注意事项

1. **高考中不能直接写"仿射变换"**——大题步骤需要用代数方法（韦达定理、判别式等）严格书写。仿射变换主要用于**小题快速求解**或**检验大题答案**。

2. **角度不保持**：在圆中得到的角度结论不能直接用于椭圆。例如 "某角为 $90^\\circ$" 在仿射后不再成立。

3. **垂直不保持**：圆中圆心与弦中点连线垂直于弦（垂径定理），但仿射回椭圆后此垂直关系不保持，只保留了斜率积为定值的结论。

4. **椭圆变为圆的两种常用变换**：
   - $x' = x/a$，$y' = y/b$：变为单位圆 $x'^2+y'^2=1$，面积缩放 $ab$
   - $x' = x$，$y' = \\frac{a}{b}y$：变为圆 $x'^2+y'^2=a^2$，面积缩放 $b/a$

   两种变换本质相同，根据问题方便选用。

5. **不适用的情况**：题目条件涉及焦点、准线、离心率时不宜使用仿射变换，因为这些量在仿射下会改变（焦点不再位于 $(\\pm c,0)$，离心率无意义）。
`;

export default lecture2_5;
