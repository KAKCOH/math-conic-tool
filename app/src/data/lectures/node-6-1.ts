// 6.1 恒成立与存在性问题 讲义
const lecture6_1 = `
## 恒成立与存在性问题

### 一、两类基本逻辑关系

设函数 $f(x)$ 在区间 $I$ 上有定义：

**恒成立问题**（全称量词 $\\forall$）：

$$\\forall x \\in I: f(x) \\ge a \\iff a \\le \\min_{x \\in I} f(x)$$

$$\\forall x \\in I: f(x) \\le a \\iff a \\ge \\max_{x \\in I} f(x)$$

**存在性问题**（存在量词 $\\exists$）：

$$\\exists x \\in I: f(x) \\ge a \\iff a \\le \\max_{x \\in I} f(x)$$

$$\\exists x \\in I: f(x) \\le a \\iff a \\ge \\min_{x \\in I} f(x)$$

> **记忆诀窍**：恒成立求最值取「最远」方向——$\\ge$ 取最小值，$\\le$ 取最大值。存在性问题取「最近」方向——$\\ge$ 取最大值，$\\le$ 取最小值。

### 二、含参恒成立问题的三大策略

#### 策略一：分离参数法（参变分离）

将不等式转化为 $a \\le g(x)$（或 $a \\ge g(x)$）的形式，则：

$$a \\le g(x) \\text{ 恒成立} \\iff a \\le g(x)_{\\min}$$

$$a \\ge g(x) \\text{ 恒成立} \\iff a \\ge g(x)_{\\max}$$

**优点**：思路清晰，将含参问题转化为求函数最值
**缺点**：$g(x)$ 的最值有时不好求（涉及隐零点、洛必达等）

**适用条件**：参数与其他变量可以完全分离到不等号两侧

#### 策略二：含参讨论法（分类讨论）

不分离参数，直接对 $f(x)$ 求导分析，讨论参数在不同范围内时 $f(x)$ 的单调性和最值。

**适用条件**：参数无法分离或分离后函数过于复杂

**标准思路**：
1. 求导 $f'(x)$，观察导函数与参数的关系
2. 确定参数的临界值（$\\Delta = 0$、零点落在区间端点等）
3. 分段讨论，每段内确定 $f(x)$ 的单调性和最小值
4. 在各段内检验恒成立条件

#### 策略三：端点效应法（必要性探路）

见节点 5.4。先由端点条件导出参数的必要范围，再验证充分性。

### 三、存在性问题的处理

存在性问题通常有两种转化思路：

1. **直接求最值**：按第一节的逻辑表直接转化为最值问题
2. **取补集**：将「存在 $x$ 使得 $P(x)$」转化为「$\\forall x$ 均有 $\\neg P(x)$」的否定

**补集法示例**：

「存在 $x \\in I$ 使 $f(x) < 0$」是「$\\forall x \\in I: f(x) \\ge 0$」的否定。因此，先求 $f(x) \\ge 0$ 恒成立的参数范围，再取补集即可。

### 四、常见参数临界值的来源

1. 端点函数值为 0 → $f($端点$) = 0 \\Rightarrow f'($端点$) \\ge 0$（或 $\\le 0$）
2. 导函数的恒正/恒负条件 → $\\Delta \\le 0$
3. 导函数零点的位置 → 与端点比较大小
4. 函数在无穷远处的行为 → 参数范围需使 $\\lim$ 符合要求

### 五、恒成立问题的验证要点

- 分离参数时，注意除以的表达式是否可能为零（需分类）
- 端点效应得出的是必要条件，必须检验充分性
- 含参讨论时，参数的每个分段都需要完整分析
- 最后结果取各分段结果的并集

### 六、任意与存在的混合模型

当题目中同时出现 $\\forall$（任意）和 $\\exists$（存在）时，需要精确理解量词逻辑。以下归纳 7 种典型模型：

设 $f(x)$、$g(x)$ 的定义域分别为 $D_f$、$D_g$，值域分别为 $R_f = \\{f(x) \\mid x \\in D_f\\}$、$R_g = \\{g(x) \\mid x \\in D_g\\}$。

**模型 ①：单一函数 + 单个「任意」**
$$\\forall x \\in D: f(x) \\ge a \\iff a \\le f_{\\min}$$

**模型 ②：单一函数 + 单个「存在」**
$$\\exists x \\in D: f(x) \\ge a \\iff a \\le f_{\\max}$$

**模型 ③：双函数 +「任意 $x_1$，存在 $x_2$」**（最常见压轴型）
$$\\forall x_1 \\in D_f,\\ \\exists x_2 \\in D_g: f(x_1) = g(x_2) \\iff R_f \\subseteq R_g$$

> 即 $f$ 的每一个值，$g$ 都能取到。等价于 $f_{\\min} \\ge g_{\\min}$ 且 $f_{\\max} \\le g_{\\max}$（当值域连通时）。

**模型 ④：双函数 +「存在 $x_1$，存在 $x_2$」**
$$\\exists x_1 \\in D_f,\\ \\exists x_2 \\in D_g: f(x_1) = g(x_2) \\iff R_f \\cap R_g \\neq \\varnothing$$

> 即 $f$ 和 $g$ 的值域有交集。等价于 $f_{\\min} \\le g_{\\max}$ 且 $f_{\\max} \\ge g_{\\min}$。

**模型 ⑤：双函数 +「任意 $x_1$，任意 $x_2$」**
$$\\forall x_1 \\in D_f,\\ \\forall x_2 \\in D_g: f(x_1) \\ge g(x_2) \\iff f_{\\min} \\ge g_{\\max}$$

**模型 ⑥：双函数 +「存在 $x_1$，任意 $x_2$」**（最难）
$$\\exists x_1 \\in D_f,\\ \\forall x_2 \\in D_g: f(x_1) \\ge g(x_2) \\iff f_{\\max} \\ge g_{\\max}$$

> 即存在 $f$ 的一个值「碾压」$g$ 的所有值。

**模型 ⑦：双函数 +「任意 $x_1$，存在 $x_2$，满足不等式」**
$$\\forall x_1 \\in D_f,\\ \\exists x_2 \\in D_g: f(x_1) \\ge g(x_2) \\iff f_{\\min} \\ge g_{\\min}$$

> 注意与模型 ⑤ 的区别：这里 $x_2$ 可以依赖 $x_1$ 变化，门槛低得多。

**解题口诀**：
- 量词「任意→任意」：最值交叉取（$f_{\\min} \\ge g_{\\max}$）
- 量词「任意→存在」：值域包含取（$R_f \\subseteq R_g$）
- 量词「存在→任意」：最值同向取（$f_{\\max} \\ge g_{\\max}$）
- 先翻译量词为最值关系，再求导处理最值
`;

export default lecture6_1;
