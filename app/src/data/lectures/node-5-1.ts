// 5.1 构造函数与同构变换 讲义
const lecture5_1 = `
## 构造函数与同构变换

### 一、构造函数的核心思路

构造函数是导数压轴题最核心的解题思想。基本模式：

1. 将问题转化为关于某个**新函数** $F(x)$ 的性质（单调性、最值、零点等）
2. 利用 $F(x)$ 的导数分析其性质，解决问题

### 二、常见构造函数模式

**模式 1：差函数**

证明 $f(x) \\ge g(x)$ $\\Rightarrow$ 构造 $F(x) = f(x) - g(x)$，证明 $F(x)_{\\min} \\ge 0$。

**模式 2：商函数**

比较 $\\frac{f(x)}{g(x)}$ 的大小关系 $\\Rightarrow$ 构造 $F(x) = \\frac{f(x)}{g(x)}$，讨论其单调性和取值范围。

**模式 3：积函数**

比较 $f(x)g(x)$ 与常数的关系 $\\Rightarrow$ 分析 $h(x) = f(x)g(x)$。

**模式 4：复合构造**

当直接构造难以处理时，利用已知不等式先放缩，再构造函数：
- 证 $\\ln(1+x) < x$（$x > 0$）可直接构造 $F(x) = x - \\ln(1+x)$
- 证 $e^x > 1 + x + \\frac{x^2}{2}$ 可构造 $F(x) = e^x - 1 - x - \\frac{x^2}{2}$，需用二阶导数

### 三、同构变换

**同构**指的是两个不同的表达式经过适当变形后，具有完全相同的函数结构，从而可以利用函数的单调性解题。

**核心技巧**：将 $f(a) = f(b)$ 或 $f(a) > f(b)$ 转化为 $a = b$ 或 $a > b$（当 $f$ 单调时）。

**常见同构类型**：

1. **指数对数同构**：$a\\ln a = b e^b$ 型
   - 利用 $e^{\\ln a} = a$，可转化为 $\\ln a \\cdot e^{\\ln a} = b e^b$
   - 构造函数 $g(t) = t e^t$，由 $g$ 的单调性得 $\\ln a = b$

2. **对数同构**：$\\frac{\\ln a}{a} = \\frac{\\ln b}{b}$ 型
   - 构造函数 $h(t) = \\frac{\\ln t}{t}$，利用其单调性

3. **差同构**：$e^a - a = e^b - b$ 型
   - 构造函数 $\\varphi(t) = e^t - t$，利用单调性

### 四、构造函数时的注意事项

1. **定义域先行**：构造新函数前，先确认原问题中变量的取值范围
2. **端点考虑**：若不等式在端点取等号，构造函数后端点值通常为 0，这有助于后续分析（端点效应）
3. **高阶导数的使用**：当一阶导数符号难以直接判断时，继续求导分析原函数的单调性

### 五、同构的识别信号

拿到题目不知道要不要用同构时，看以下几个**典型信号**：

**信号 1：指数与对数「捆绑」出现**

看到 $xe^x$、$\\frac{e^x}{x}$、$x\\ln x$、$\\frac{\\ln x}{x}$ 这类指对混合的积商形式，优先考虑同构。

**信号 2：指数与对数分居等式（不等式）两侧**

如 $e^{a} + a = \\ln b + b$ 型，两侧各含指/对函数，且「函数+自变量」结构相似。

**信号 3：$e^{kx}$ 与 $x$ 的乘积组合**

$xe^{2x}$、$x^2 e^x$ 等，可通过 $x = e^{\\ln x}$ 代换化为统一指数形式。

**核心互化公式**（必须熟练）：

$$x = e^{\\ln x} \\quad (x > 0)$$
$$xe^x = e^{x + \\ln x}$$
$$\\frac{e^x}{x} = e^{x - \\ln x}$$
$$x + \\ln x = \\ln(x e^x)$$

**同构的三种基本模板**：

| 模板 | 结构 | 构造函数 |
|------|------|---------|
| 和型 | $e^x + x \\sim \\ln y + y$ | $g(t) = e^t + t$（严格增） |
| 积型 | $xe^x \\sim y \\ln y$ | $g(t) = t e^t$ 或 $g(t) = t \\ln t$ |
| 商型 | $\\frac{e^x}{x} \\sim \\frac{y}{\\ln y}$ | $g(t) = \\frac{e^t}{t}$ 或 $g(t) = \\frac{t}{\\ln t}$ |

**识别后的操作流**：
1. 将不等式两端变形为同一函数 $g$ 在 $A$ 处和 $B$ 处的值：$g(A) \\sim g(B)$
2. 分析 $g(t)$ 的单调性（通常单调增或单调减）
3. 利用单调性将 $g(A) > g(B)$ 转化为 $A > B$（或反向），消去超越函数，留下代数不等式

### 六、经典例题

**例 1**：比较 $e^{\\pi}$ 与 $\\pi^{e}$ 的大小。

> 构造 $f(x) = \\frac{\\ln x}{x}$，$f'(x) = \\frac{1 - \\ln x}{x^2}$，$f_{\\max} = f(e) = \\frac{1}{e}$。因 $\\pi > e$，$f(\\pi) < f(e)$，得 $\\frac{\\ln \\pi}{\\pi} < \\frac{1}{e} \\Rightarrow \\pi^{e} < e^{\\pi}$。

**例 2**：解方程 $e^x + x = 2$。

> $g(x) = e^x + x$ 严格增（$g'(x) = e^x + 1 > 0$），由 $g(0) = 1 < 2$ 且 $g(\\ln 2) = 2 + \\ln 2 > 2$，知方程有唯一解。此即同构思想——利用单调函数建立一一对应。
`;

export default lecture5_1;
