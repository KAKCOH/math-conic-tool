// 6.2 双变量与多变量问题 讲义
const lecture6_2 = `
## 双变量与多变量问题

### 一、双变量问题的本质

双变量问题指的是题目中同时出现两个独立变量（如 $x_1$、$x_2$），需要证明关于它们的不等式或求取值范围。核心挑战是**将双变量问题转化为单变量问题**。

### 二、消元法——利用等式关系

当两个变量之间满足某个等式关系（如 $f(x_1) = f(x_2)$、$x_1 + x_2 = k$ 等），可利用此等式**消去一个变量**，将问题转化为单变量函数问题。

**典型场景**：
- 韦达定理：$x_1 + x_2$、$x_1x_2$ 已知，对称式可整体表达
- 等式约束：$x_1 + x_2 = 2$，则 $x_2 = 2 - x_1$，代入消元

### 三、比值代换法——设 $t = \\frac{x_2}{x_1}$

这是双变量问题中最常用的技巧，尤其适用于 $f(x_1) = f(x_2)$ 型问题。

**步骤**：
1. 不妨设 $x_1 < x_2$，令 $t = \\frac{x_2}{x_1} > 1$
2. 利用 $f(x_1) = f(x_2)$ 得到 $x_1$ 与 $x_2$ 的关系
3. 将 $x_2 = tx_1$ 代入，解出 $x_1 = g_1(t)$，$x_2 = g_2(t)$
4. 将待证不等式转化为仅含 $t$（$t > 1$）的形式
5. 构造函数 $h(t)$，利用导数证明

**示例思路**（$f(x) = \\ln x - x$，$f(x_1) = f(x_2)$）：

> 由 $\\ln x_1 - x_1 = \\ln x_2 - x_2$ 得 $x_2 - x_1 = \\ln(x_2/x_1) = \\ln t$。
> 令 $t = x_2/x_1 > 1$，则 $x_2 = tx_1$，代入得 $(t-1)x_1 = \\ln t$。
> $x_1 = \\frac{\\ln t}{t-1}$，$x_2 = \\frac{t\\ln t}{t-1}$。
> 证 $x_1 + x_2 > 2$ 即 $\\frac{(t+1)\\ln t}{t-1} > 2$，转为 $t$ 的单变量不等式。

### 四、主元法——选定一个变量为「主元」

当两个变量地位不对称时，可**固定其中一个变量**，将另一个视为参数，对选定为主元的变量求导分析。

**适用场景**：
- 二元函数的不等式证明
- 一个变量在给定区间上变动，另一个作为约束

**步骤**：
1. 将 $x$ 视为主变量，$y$ 视为参数
2. 构造函数 $G(x) = F(x, y)$（$y$ 固定）
3. 对 $x$ 求导，证明 $G(x)$ 的单调性或最值
4. 再利用 $y$ 的条件完成最终结论

### 五、对称化方法

对于具有对称结构的问题（如 $x_1$ 与 $x_2$ 互换后表达式不变），可利用对称性简化。

**策略**：
- 不妨设 $x_1 < x_2$（或 $x_1 \\le x_2$），将无序变量转为有序
- 用 $s = x_1 + x_2$ 和 $p = x_1x_2$ 表示对称式（注意 $s$ 和 $p$ 的关系由基本不等式约束）
- 利用均值不等式等进行放缩

### 六、常用工具汇总

| 工具 | 使用条件 | 效率 |
|------|---------|------|
| 消元法 | 两变量有等式约束 | 直接 |
| 比值代换 | $f(x_1) = f(x_2)$ + 变量同号 | 通用 |
| 主元法 | 变量地位不对称 | 灵活 |
| 对称化 | 表达式对称 + 有自然大小关系 | 简洁 |
| 均值不等式 | 对称正变量 + 和/积约束 | 省去求导 |
| 对数平均不等式 | 涉及 $\\frac{x_2 - x_1}{\\ln x_2 - \\ln x_1}$ | 直接套用 |
`;

export default lecture6_2;
