// 3.1 联立与参数法 讲义
const lecture3_1 = `
## 联立与参数法

### 一、核心解题三部曲（大题通用）

几乎所有圆锥曲线大题的第一步都是以下三步：

**第一步：一设**
- 设交点 $A(x_1, y_1)$、$B(x_2, y_2)$
- 设直线方程 $y = kx + b$（**注意讨论 $k$ 不存在的情况**）
- 若直线过定点 $(x_0, y_0)$，写为 $y - y_0 = k(x - x_0)$，减少一个参数
- 若设 $x = my + n$，可以回避斜率不存在的讨论（适用于抛物线或焦点在 $x$ 轴的情况）

**第二步：二联立**
- 将直线方程代入圆锥曲线方程，消元得到关于 $x$（或 $y$）的一元二次方程
- 整理成标准形式 $Ax^2 + Bx + C = 0$（或 $Ay^2 + By + C = 0$）

**第三步：三韦达**
- 计算判别式 $\\Delta = B^2 - 4AC > 0$（确保相交）
- 写出根与系数的关系：
$$x_1 + x_2 = -\\frac{B}{A}, \\quad x_1 x_2 = \\frac{C}{A}$$

> **常见丢分点**：忘记 $\\Delta > 0$ 的存在性检验；忘记讨论斜率不存在。

### 二、硬解定理（快速联立公式）

对于椭圆/双曲线 $\\frac{x^2}{m} + \\frac{y^2}{n} = 1$ 与直线 $Ax + By + C = 0$ 联立：

$$(nA^2 + mB^2)x^2 + 2mACx + m(C^2 - nB^2) = 0$$

$$\\Delta = 4mn(nA^2 + mB^2 - C^2)$$

其中 $m = a^2$ 或 $b^2$，$n = b^2$ 或 $-b^2$（双曲线时 $n$ 取负）。

> **记忆技巧**：将 $y = -\\frac{Ax + C}{B}$ 代入 $\\frac{x^2}{m} + \\frac{y^2}{n} = 1$，通分整理即得。考试中直接推导也可，不要求死记。

### 三、参数法：用一个变量代替两个

参数法的核心思想：**用一个参数表示点坐标，减少方程中变量的个数**。

**三种标准参数方程：**

| 曲线 | 参数方程 | 参数含义 |
|------|---------|---------|
| 椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ | $\\begin{cases} x = a\\cos\\theta \\\\ y = b\\sin\\theta \\end{cases}$ | $\\theta$ 为离心角 |
| 双曲线 $\\frac{x^2}{a^2}-\\frac{y^2}{b^2}=1$ | $\\begin{cases} x = a\\sec\\theta \\\\ y = b\\tan\\theta \\end{cases}$ | $\\theta$ 为参数角 |
| 抛物线 $y^2 = 2px$ | $\\begin{cases} x = 2pt^2 \\\\ y = 2pt \\end{cases}$ | $t$ 无特定几何含义 |

**参数法的适用场景**：
- 点在曲线上移动，需要表示该点的坐标（消去 $x,y$ 的耦合）
- 距离、斜率的最值问题（转化为三角函数求值域）
- 涉及角度的条件（利用 $\\theta$ 与角的自然对应）

**椭圆参数法求最值示例**：

设 $P(a\\cos\\theta, b\\sin\\theta)$ 在椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ 上，则：
- $|OP|^2 = a^2\\cos^2\\theta + b^2\\sin^2\\theta = a^2 - (a^2-b^2)\\sin^2\\theta$，可求范围
- $P$ 到直线 $Ax+By+C=0$ 的距离 $d = \\frac{|Aa\\cos\\theta + Bb\\sin\\theta + C|}{\\sqrt{A^2+B^2}}$，用辅助角公式求最值

**抛物线参数法的独特优势**：
- $P(2pt^2, 2pt)$ 处切线方程为 $y \\cdot 2pt = p(x + 2pt^2)$，即 $y = \\frac{1}{2t}x + pt$
- 过 $P$ 的切线斜率为 $\\frac{1}{2t}$，与参数 $t$ 直接对应

### 四、方法选择指南

| 问题类型 | 推荐方法 | 原因 |
|---------|---------|------|
| 直线与曲线交于两点，涉及 $x_1+x_2$、$x_1x_2$ | 联立+韦达 | 直接有效 |
| 点 $(x_0,y_0)$ 在某曲线上移动，求与 $x_0,y_0$ 相关表达式的最值 | 参数法 | 单变量化 |
| 涉及角度的条件 | 参数法 | $\\theta$ 与角对应 |
| 已知直线过定点 | 设点斜式 | 减少参数 |
| 双曲线或抛物线中回避斜率讨论 | 设 $x = my + n$ | 简化讨论 |
`;
export default lecture3_1;
