// 3.6 定点定值与最值范围 讲义
const lecture3_6 = `
## 定点定值与最值范围

圆锥曲线大题的高频压轴题型。解题思想归纳为八个字：**先猜后证，设参消参**。

### 一、定点问题

**问题特征**：某几何对象（直线、圆等）随参数变化，但恒过某个定点。

**方法一：特殊 $\\to$ 一般法（推荐先做）**

1. 取两个特殊位置（如斜率不存在、过顶点等），分别求出几何对象
2. 联立两个特殊位置的结果，猜出定点坐标
3. 证明一般情形下该几何对象过此定点

> 即使最终证明不完整，**先猜出答案**也能拿到大部分分数。

**方法二：设参消参法（通用严格解法）**

1. 设出参数（通常是直线的斜率 $k$ 或截距 $m$）
2. 将几何对象的方程写成参数的多项式形式：
   $$f(k) \\cdot x + g(k) \\cdot y + h(k) = 0$$
3. 将方程整理为**关于参数的恒等式**：
   $$k \\cdot A(x,y) + B(x,y) = 0$$
4. 令参数的各次幂系数为零：
   $$\\begin{cases} A(x,y) = 0 \\\\ B(x,y) = 0 \\end{cases}$$
5. 解此方程组，所得 $(x,y)$ 即为定点坐标

**关键技巧**：整理恒等式时，把含参项归并，不含参的常数项归并，做到"参数的所有系数都为零"。

**常见定点结论速记**：
- 椭圆上任意弦被 $x$ 轴上某定点平分 → 弦中点轨迹过椭圆中心（原点）
- 过椭圆焦点的弦，两端点处的切线交点在准线上
- 抛物线上两点 $A$、$B$ 满足 $OA \\perp OB$ → 直线 $AB$ 过定点 $(2p, 0)$

### 二、定值问题

**问题特征**：某几何量（斜率之和/积、线段长度、面积、向量数量积等）不随参数变化。

**方法一：先猜后证法**

1. 取特殊位置（如斜率不存在、直线过顶点等）计算出该量的值
2. 在一般情形下化简表达式，证明结果与特殊情形一致

**方法二：直接消元法**

在代数推导过程中，参数自然消去，得到常数。特点是运算量大但思路直接。

**常见定值结论速记**：

| 条件 | 定值 |
|------|------|
| 椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ 上任意点 $P$：$k_{PA} \\cdot k_{PB}$（$A,B$ 为长轴端点） | $-\\frac{b^2}{a^2}$ |
| 抛物线 $y^2=2px$ 焦点弦 $AB$：$x_1x_2$ | $\\frac{p^2}{4}$ |
| 抛物线 $y^2=2px$ 焦点弦 $AB$：$y_1y_2$ | $-p^2$ |
| 抛物线 $y^2=2px$ 焦点弦 $AB$：$\\frac{1}{|AF|}+\\frac{1}{|BF|}$ | $\\frac{2}{p}$ |

### 三、最值与范围问题：两大路径 + 四大技巧

**路径一：几何法**

利用圆锥曲线的几何性质直观求解，不需要大量代数运算。参见 3.2 节（定义法与数形结合）。

典型例子：
- 椭圆上点到焦点距离的范围：$[a-c, a+c]$
- 椭圆上点到中心距离的范围：$[b, a]$
- 双曲线上点到渐近线距离的最小值（用距离公式+基本不等式）

**路径二：代数法（四大技巧）**

**技巧1：利用判别式 $\\Delta$ 构造不等式**

由 $\\Delta \\geq 0$（或 $\\Delta > 0$）得到参数的约束范围，进而求得目标量的取值范围。

> 思路最自然：存在性的必要条件 $\\Delta \\geq 0$ 本身就是一个不等式，直接给出参数范围。

**技巧2：建立目标函数求值域**

将目标量表示为某一参数的函数 $y = f(t)$，求该函数的值域。
- 常用函数类型：二次函数、分式函数、根式函数
- 注意定义域（来自 $\\Delta > 0$、参数天然范围等）

**技巧3：利用基本不等式**

$$a + b \\geq 2\\sqrt{ab} \\quad (a,b > 0)$$
$$ab \\leq \\left(\\frac{a+b}{2}\\right)^2$$

适用场景：目标量可以写成和或积的形式，变量均为正数。

**技巧4：利用已知参数范围建立不等关系**

题目中往往隐藏了参数的范围条件（如点在椭圆内、离心率 $e \\in (0,1)$、$a > b > 0$ 等），将这些条件转化为不等式。

**典型题路：面积最值问题**
（详见 3.4 节第四部分，此处仅列要点）
1. 联立 $\\to$ 弦长 $\\\\to$ 距离 $\\\\to$ 面积表达式
2. 将面积化为 $S = f(t)$
3. 求 $f(t)$ 的最值（基本不等式 / 二次函数 / 导数）

### 四、解题策略总结

| 题型 | 第一反应 | 核心操作 |
|------|---------|---------|
| 定点 | 特殊位置猜定点 | 设参 $\\to$ 整理多项式 $\\\\to$ 参数系数=0 |
| 定值 | 特殊位置猜数值 | 设参 $\\to$ 化简 $\\\\to$ 消参得常数 |
| 最值 | 先看能否几何法 | $\\Delta$ 约束 / 值域 / 基本不等式 |
| 范围 | $\\Delta \\geq 0$ 起步 | 联立 $\\\\to$ $\\Delta$ $\\\\to$ 参数范围 $\\\\to$ 目标范围 |

### 五、常见易错点

- 定点问题中，整理参数恒等式时**常数项容易遗漏**——把 $x,y$ 和常量分开，逐项归类
- 定值问题中，特殊位置求出的值**一定要验证一般情形**——特殊情形猜对了不代表证明完成
- 最值问题中，用基本不等式时注意**等号成立条件**是否能在定义域内满足
- 参数范围问题中，约束条件可能来自多个方面（$\\Delta \\geq 0$、点在曲线内部、参数天然定义域等），容易漏掉某个约束
`;
export default lecture3_6;
