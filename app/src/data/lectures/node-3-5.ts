// 3.5 齐次化联立与非对称韦达 讲义
const lecture3_5 = `
## 齐次化联立与非对称韦达

### 一、齐次化联立：处理斜率关系

**适用场景（核心判据）**：过一定点作两条直线，两条直线与曲线各有一个交点，且两条直线的**斜率**之间存在某种关系（和为定值、积为定值、相等）。

**标准操作流程**：

**步骤1：将定点平移至原点**

若定点为 $P(x_0, y_0)$，令 $\\begin{cases} x' = x - x_0 \\\\ y' = y - y_0 \\end{cases}$。在新坐标系中，定点变为原点 $O'$，过原点的两条直线方程可写为 $y' = k_1 x'$ 和 $y' = k_2 x'$。

![齐次化联立：坐标平移，定点移至原点](/figures/homogenization_translate.png)

**步骤2：对曲线方程变形**

将曲线的原方程按 $x'$、$y'$ 展开，整理成：
$$Ax'^2 + Bx'y' + Cy'^2 + Dx' + Ey' + F = 0$$

**步骤3：构造齐次式**

将两条直线表示为 $y' = kx'$，即 $k = \\frac{y'}{x'}$。代入曲线方程，将一次项和常数项配成 $x'^2$ 的系数：
- 将 $Dx' + Ey'$ 写成 $Dx' \\cdot 1 + Ey' \\cdot 1$，再将 $1$ 用直线方程替换
- 最终得到关于 $\\frac{y'}{x'}$（即 $k$）的二次方程

**步骤4：利用韦达定理得斜率关系**

整理后得到关于 $k$ 的方程 $Ak^2 + Bk + C = 0$，则：
$$k_1 + k_2 = -\\frac{B}{A}, \\quad k_1 k_2 = \\frac{C}{A}$$

**关键注意事项**：
- 齐次化过程中要确保直线方程中不出现截距项（因为平移后过原点）
- 最后**必须验证 $\\Delta > 0$**，不能因为用了齐次化就跳过判别式
- 如果定点不在曲线上，一次项 $Dx' + Ey'$ 非零，需要配凑

**进阶：齐次化 + 斜率同构处理相切类问题**

当需要证明"存在两条切线且切线斜率满足某关系"时，可设切线为 $y = kx + m$，利用相切条件 $\\Delta=0$ 得到 $k$ 与 $m$ 的关系，再将两切线斜率视为某二次方程的两根，用齐次化得出结果。

### 二、非对称韦达定理：七种处理方法

**问题特征**：表达式中的 $x_1$、$x_2$ 系数不对等，例如需要求 $2x_1 + x_2$、$\\frac{x_1}{x_2}$、$x_1^2 x_2$ 等——无法直接套用 $x_1+x_2$ 和 $x_1x_2$。

**方法一：倒数相加法（$x_1/x_2$ 型）**

利用恒等式：
$$\\frac{x_1}{x_2} + \\frac{x_2}{x_1} = \\frac{(x_1+x_2)^2}{x_1x_2} - 2$$

已知 $x_1+x_2$ 和 $x_1x_2$，可求得 $\\frac{x_1}{x_2} + \\frac{x_2}{x_1}$，再设 $t = \\frac{x_1}{x_2}$，解 $t + \\frac{1}{t}$ 的方程。

**方法二：配常数 + 倒数相加（$y_2 = ky_1 + b$ 型）**

当涉及 $\\frac{y_1}{y_2}$ 且两点在直线 $y = kx + b$ 上时，将 $\\frac{y_1}{y_2}$ 用 $x_1$、$x_2$ 表示后，通过配凑常数将非对称比对称化。

**方法三：设系数法 / 和积互化（$\\lambda(x_1+x_2) = x_1x_2$ 型）**

如果目标表达式可化为 $\\lambda(x_1+x_2) = x_1x_2$ 的形式，代入韦达定理直接解出 $\\lambda$。

**方法四：曲线方程消元法**

利用 $A(x_1, y_1)$ 在曲线上这一条件，用曲线方程消去 $x_1^2$（或 $y_1^2$）项，降低非对称度。

**方法五：消元留一法（通法，最稳健）**

由韦达定理 $x_1 + x_2 = -\\frac{B}{A}$，解出 $x_2 = -\\frac{B}{A} - x_1$，代入 $x_1x_2 = \\frac{C}{A}$ 消去 $x_2$，将目标表达式化为仅含 $x_1$ 的单变量式子。再根据需要配合其他条件求解。此法普适性最强，适用于绝大多数非对称情况。

**方法六：带点转化法**

利用题目给定的定点坐标代入关系式，替换掉不对称的系数。

**方法七：参数方程法**

用椭圆参数方程 $P(a\\cos\\theta, b\\sin\\theta)$ 表示点坐标，用单变量 $\\theta$ 替代双变量 $x_1,x_2$，从根本上绕开韦达定理。这是处理极难非对称问题的"核武器"——代价是三角函数运算。

### 三、齐次化 vs 普通联立

| 条件 | 推荐方法 | 原因 |
|------|---------|------|
| 过定点 + 两直线与曲线各交一点 + 斜率关系 | 齐次化 | 直接得到 $k$ 的二次方程 |
| 一条直线与曲线交于两点 | 普通联立+韦达 | 齐次化不适用 |
| 两条直线但无斜率条件 | 普通联立 | 齐次化优势不发挥 |
| 定点在 $x$ 轴上 + 线段比 | 定比点差法（3.3节） | 绕过坐标运算 |

### 四、常见易错点

- 齐次化平移后**忘记移回去**——最终斜率是在新坐标系中的 $\\frac{y'}{x'}$，需验证其与原始坐标系中的斜率是否一致（一致，因为平移不改变斜率）
- 齐次化过程中，如果定点**不**在曲线上，一次项 $Dx' + Ey'$ 非零，必须用截距条件配凑，不能直接忽略
- 非对称处理中"消元留一法"最通用，但也最笨重——遇到具体题目时先看看是否有更简洁的方法
- 不管用什么方法，最后都要回到 $\\Delta > 0$ 的条件上
`;
export default lecture3_5;
