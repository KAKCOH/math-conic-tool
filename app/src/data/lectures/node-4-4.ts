// 4.4 导数与极值、最值 讲义
const lecture4_4 = `
## 导数与极值、最值

### 一、极值的定义

设函数 $f(x)$ 在 $x_0$ 的某邻域内有定义：

- **极大值**：若存在 $\\delta > 0$，使得对所有 $x \\in (x_0-\\delta, x_0+\\delta)$，均有 $f(x) \\le f(x_0)$，则称 $f(x_0)$ 为 $f(x)$ 的一个极大值。
- **极小值**：若恒有 $f(x) \\ge f(x_0)$，则称 $f(x_0)$ 为 $f(x)$ 的一个极小值。

> 极值是**局部性质**，最值是**整体性质**。极大值不一定大于极小值（如图像有多个「山丘」时，一个「山丘」可能比另一个「山谷」还低）。

### 二、极值的必要条件

若 $f(x)$ 在 $x_0$ 处可导且取极值，则 $f'(x_0) = 0$。

> **逆命题不成立**：$f'(x_0) = 0$ 不保证 $x_0$ 是极值点。反例：$f(x) = x^3$，$f'(0) = 0$ 但 $x=0$ 不是极值点。

驻点：满足 $f'(x_0) = 0$ 的点称为**驻点**（或临界点）。

### 三、极值的充分条件

**第一充分条件**（一阶导数符号法）：

若 $f'(x_0) = 0$，且在 $x_0$ 两侧 $f'(x)$ 变号：
- 左正右负 $\\Rightarrow$ $x_0$ 为极大值点
- 左负右正 $\\Rightarrow$ $x_0$ 为极小值点
- 不变号 $\\Rightarrow$ $x_0$ 不是极值点

**第二充分条件**（二阶导数法）：

若 $f'(x_0) = 0$ 且 $f''(x_0)$ 存在：
- $f''(x_0) < 0 \\Rightarrow x_0$ 为极大值点
- $f''(x_0) > 0 \\Rightarrow x_0$ 为极小值点
- $f''(x_0) = 0 \\Rightarrow$ 需用第一充分条件或更高阶导数判断

### 四、求极值的标准步骤

1. 确定定义域
2. 求 $f'(x)$，解 $f'(x) = 0$ 和 $f'(x)$ 不存在的点
3. 列表检验各临界点两侧 $f'(x)$ 的符号（或计算 $f''(x_0)$）
4. 确定极值点与极值

### 五、闭区间上的最值

设 $f(x)$ 在 $[a, b]$ 上连续，则最值必存在。求法：

1. 求 $f(x)$ 在 $(a, b)$ 内的所有驻点和不可导点
2. 计算这些点及端点 $a$、$b$ 处的函数值
3. 比较所有值，最大者为最大值，最小者为最小值

> 若连续函数在区间内只有一个极值，则该极值就是最值。

### 六、极值的参数问题

已知函数在某点取极值，求参数值的步骤：
1. 由 $f'(x_0) = 0$ 解出参数
2. **检验充分性**（一阶或二阶条件）——这是必做步骤，否则可能出错
3. 若还需确定是极大还是极小，再用充分条件判断

> 常见陷阱：由 $f'(x_0) = 0$ 解出的参数值中，可能有不使 $x_0$ 为极值点的值（如 $f''(x_0) = 0$ 的情况），必须检验。
`;

export default lecture4_4;
