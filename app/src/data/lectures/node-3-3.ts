// 3.3 点差法与定比点差 讲义
const lecture3_3 = `
## 点差法与定比点差

### 一、点差法：中点弦问题的首选

**适用场景**：已知弦的中点（或与中点相关的条件），求弦所在直线的斜率或方程。

**标准四步操作**：

1. 设弦端点 $A(x_1, y_1)$、$B(x_2, y_2)$，均在曲线上
2. 将两点分别代入曲线方程，得到两个等式
3. 两式相减，利用平方差公式 $x_1^2 - x_2^2 = (x_1-x_2)(x_1+x_2)$ 因式分解
4. 整理出 $\\frac{y_1-y_2}{x_1-x_2}$（即弦的斜率 $k_{AB}$）与中点坐标的关系

**各曲线的中点弦公式**（设弦 $AB$ 的中点为 $M(x_0, y_0)$）：

| 曲线 | 中点弦公式 |
|------|-----------|
| 椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ | $k_{AB} \\cdot k_{OM} = -\\frac{b^2}{a^2}$ |
| 双曲线 $\\frac{x^2}{a^2}-\\frac{y^2}{b^2}=1$ | $k_{AB} \\cdot k_{OM} = \\frac{b^2}{a^2}$ |
| 抛物线 $y^2 = 2px$ | $y_0 \\cdot k_{AB} = p$ |

其中 $k_{OM} = \\frac{y_0}{x_0}$ 是原点与弦中点连线的斜率。

**推导示例（椭圆）**：
$$\\frac{x_1^2}{a^2}+\\frac{y_1^2}{b^2}=1,\\quad \\frac{x_2^2}{a^2}+\\frac{y_2^2}{b^2}=1$$
两式相减：
$$\\frac{(x_1-x_2)(x_1+x_2)}{a^2}+\\frac{(y_1-y_2)(y_1+y_2)}{b^2}=0$$
代入 $x_1+x_2=2x_0$、$y_1+y_2=2y_0$，整理即得 $\\frac{y_1-y_2}{x_1-x_2} \\cdot \\frac{y_0}{x_0} = -\\frac{b^2}{a^2}$。

**点差法的优点**：
- 绕过联立方程组，避免解一元二次方程
- 直接得到中点与斜率的关系，计算量远小于韦达法

**点差法的局限**：
- 只适用于"弦的中点"相关条件
- 点差法求出的弦**仍需验证 $\\Delta > 0$** —— 即验证弦是否真实存在（两端点是否不重合且都在曲线内）
- 如果中点不在曲线内部，弦不存在

### 二、定比点差法（轴点差）：非对称结构的利器

当条件和所求量可以写成 $\\frac{y_1}{y_2} = \\frac{x_1 + a}{x_2 + b}$ 等形式时，直接使用韦达定理计算会很繁琐——因为 $x_1$ 和 $x_2$ 的地位不对称。定比点差法专门处理这类问题。

**核心概念**：

设 $B(x_B, 0)$ 为 $x$ 轴上的定点，其关于椭圆 $\\frac{x^2}{a^2}+\\frac{y^2}{b^2}=1$ 的**调和共轭点**为：

$$I\\left(\\frac{a^2}{x_B}, 0\\right)$$

$B$ 与 $I$ 互为调和共轭点，满足 $OB \\cdot OI = a^2$。

**定比点差法的操作流程**：

1. 设 $\\overrightarrow{AP} = \\lambda \\overrightarrow{PB}$，其中 $P$ 在曲线上，$A$、$B$ 在 $x$ 轴上
2. 将 $A$、$B$ 两点坐标代入曲线方程
3. 利用向量的比例关系消去 $P$，通过合比配凑直接得到 $\\lambda$ 与其他量的关系
4. **关键优势**：不需要解出 $x_1$、$x_2$ 的具体值，直接得到比值

**适用题型**：
- $x$ 轴上的定点引出的弦，条件和结论涉及线段比
- 过焦点的弦被 $x$ 轴上的某点分成定比
- $A$、$B$、$P$ 三点共线且两点在坐标轴上

### 三、点差法 vs 韦达法：如何选

| 条件 | 推荐方法 | 原因 |
|------|---------|------|
| 知道弦中点坐标 | 点差法 | 一步得斜率 |
| 知道弦所在直线方程，求弦中点轨迹 | 韦达法（先求中点坐标表达式再消参） | 点差法不直接给出 |
| 条件不涉及中点 | 韦达法 | 点差法不适用 |
| 轴上的定点 + 线段比例 | 定比点差法 | 绕过不对称结构 |

### 四、常见易错点

- **点差法求出的弦要验证存在性**（$\\Delta > 0$ 且中点坐标满足椭圆内部条件 $\\frac{x_0^2}{a^2}+\\frac{y_0^2}{b^2} < 1$）
- 椭圆和双曲线的中点弦公式符号相反：椭圆是 $-\\frac{b^2}{a^2}$，双曲线是 $+\\frac{b^2}{a^2}$
- 抛物线 $y^2=2px$ 的中点弦是 $y_0 k_{AB} = p$（中点在对称轴上的情况需单独讨论）
- 定比点差法中，调和共轭点 $I(a^2/x_B, 0)$ 只有当 $x_B \\neq 0$ 时有定义
`;
export default lecture3_3;
