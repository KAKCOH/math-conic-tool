// 5.3 极值点偏移 讲义
const lecture5_3 = `
## 极值点偏移

### 一、极值点偏移的定义

设函数 $f(x)$ 在 $x = x_0$ 处取极值，若存在 $x_1 \\neq x_2$ 使得 $f(x_1) = f(x_2)$：

- **不偏移**：$\\frac{x_1 + x_2}{2} = x_0$（对称分布）
- **左偏**：$\\frac{x_1 + x_2}{2} < x_0$（图像左侧「陡」、右侧「缓」）
- **右偏**：$\\frac{x_1 + x_2}{2} > x_0$（图像左侧「缓」、右侧「陡」）

> 极值点偏移的本质是函数的**不对称性**。如果 $f(x)$ 关于 $x = x_0$ 对称，则不会有偏移。偏移量的大小反映了函数在极值点两侧「下降/上升」速度的差异。

### 二、两类主要证明方法

#### 方法一：对称化构造法

**核心思路**：要证 $x_1 + x_2 > 2x_0$（或 $<$），构造 $F(x) = f(x) - f(2x_0 - x)$。

设 $x_1 < x_0 < x_2$，$2x_0 - x_1$ 是 $x_1$ 关于极值点的对称点。若能证明：

$$f(x_2) - f(2x_0 - x_1) < 0 \\text{ 且 } f \\text{ 在 } (x_0, +\\infty) \\text{ 上单调}$$

则 $x_2 > 2x_0 - x_1$，即 $x_1 + x_2 > 2x_0$。

**步骤**：
1. 确定极值点 $x_0$ 和 $f$ 在 $x_0$ 两侧的单调性
2. 构造 $F(x) = f(x) - f(2x_0 - x)$（$x < x_0$）
3. 求导分析 $F(x)$ 的符号（通常 $F(x) < 0$）
4. 利用单调性完成证明

#### 方法二：比值代换法

**核心思路**：设 $t = \\frac{x_2}{x_1}$（$t > 1$），利用 $f(x_1) = f(x_2)$ 消去一个变量。

由 $f(x_1) = f(x_2)$ 可得关于 $x_1, x_2$ 的等式，再结合 $x_2 = tx_1$，解出 $x_1$ 和 $x_2$ 用 $t$ 表示，将 $x_1 + x_2$ 转化为 $t$ 的单变量函数。

**步骤**：
1. 由 $f(x_1) = f(x_2)$ 建立方程
2. 令 $t = \\frac{x_2}{x_1} > 1$，代入消元
3. 解出 $x_1 = g_1(t)$，$x_2 = g_2(t)$
4. 将结论转化为关于 $t$（$t > 1$）的单变量函数不等式
5. 构造函数 $h(t)$，利用导数证明

### 三、两种方法的适用场景对比

| 场景 | 推荐方法 |
|------|---------|
| 函数形式简单（如 $f(x) = \\ln x - x$、$f(x) = xe^{-x}$）| 两种均可，比值代换更直接 |
| 极值点 $x_0$ 已知且较好计算对称点 | 对称化构造法 |
| $f(x_1) = f(x_2)$ 可转化为 $x_1$ 与 $x_2$ 的显式关系 | 比值代换法 |
| $f(x) = g(x)e^x$ 型 | 比值代换更自然（取对数后简化）|

### 四、对数平均不等式（ALG 不等式）

极值点偏移问题中，对数平均不等式是最强有力的工具之一。

对 $x_1, x_2 > 0$ 且 $x_1 \\neq x_2$，记 $L = \\frac{x_1 - x_2}{\\ln x_1 - \\ln x_2}$ 为**对数平均**：

$$\\sqrt{x_1 x_2} < L < \\frac{x_1 + x_2}{2}$$

即：**几何平均 < 对数平均 < 算术平均**。

#### 证明方法一：比值代换法（设 $t = \\frac{x_2}{x_1} > 1$）

不妨设 $x_2 > x_1 > 0$，令 $t = \\frac{x_2}{x_1} > 1$。

**证右半** $L < \\frac{x_1 + x_2}{2}$：等价于 $\\ln t > \\frac{2(t-1)}{t+1}$。构造 $\\varphi(t) = \\ln t - \\frac{2(t-1)}{t+1}$，求导得 $\\varphi'(t) = \\frac{(t-1)^2}{t(t+1)^2} > 0$（$t > 1$），且 $\\varphi(1) = 0$，故 $\\varphi(t) > 0$，右半得证。

**证左半** $\\sqrt{x_1 x_2} < L$：等价于 $\\ln t > \\frac{t-1}{\\sqrt{t}}$ 即 $\\ln t > \\sqrt{t} - \\frac{1}{\\sqrt{t}}$。令 $s = \\sqrt{t} > 1$，化为 $\\ln s > \\frac{s - s^{-1}}{2}$（$s > 1$）。构造 $\\psi(s) = \\ln s - \\frac{s - s^{-1}}{2}$，$\\psi'(s) = -\\frac{(s-1)^2}{2s^2} < 0$，但 $\\psi(1) = 0$，故 $\\psi(s) < 0$。实际上左半等价于 $\\frac{\\ln t}{t-1} < \\frac{1}{\\sqrt{t}}$，同样构造函数可证。

#### 证明方法二：定积分法（更直观）

$\\ln t = \\int_1^t \\frac{1}{u} du$。由 $\\frac{2}{u+1} < \\frac{1}{u} < \\frac{u+1}{2u\\sqrt{u}}$（$u > 0$），积分后即得 ALG 不等式。此法是 2018 年全国 I 卷的命题背景。

#### 四类典型应用

| 应用场景 | 如何使用 | 例题特征 |
|----------|---------|---------|
| **极值点偏移** | 从 $f(x_1) = f(x_2)$ 推出 $\\frac{x_1 - x_2}{\\ln x_1 - \\ln x_2}$ 形式的表达式，用 ALG 直接放缩 | 已知两函数值相等，证 $x_1 + x_2$ 或 $x_1 x_2$ 相关不等式 |
| **双零点问题** | 零点满足 $f(x_1) = f(x_2) = 0$，化简后常出现对数差 | $\\ln x_1 - ax_1 = \\ln x_2 - ax_2$ 型 |
| **不等式放缩** | 将复杂分式用 ALG 替换为更简单的代数分式 | 含 $\\frac{a-b}{\\ln a - \\ln b}$ 的不等式 |
| **参数范围** | 将参数表示为对数平均的形式，用 ALG 上下界锁定范围 | $\\lambda < \\frac{x_1 - x_2}{\\ln x_1 - \\ln x_2}$ 型 |

> **记忆口诀**：对均不等夹中间，几何下界算术上；比值代换证两半，构造函数一招通。
`;

export default lecture5_3;
