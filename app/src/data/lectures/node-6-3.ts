// 6.3 导数与不等式证明 讲义
const lecture6_3 = `
## 导数与不等式证明

### 一、构造函数法——标准范式

证明不等式 $f(x) > g(x)$（$x \\in I$）的标准范式：

1. 构造 $F(x) = f(x) - g(x)$
2. 对 $F(x)$ 求导，分析单调性和最值
3. 若有 $F(x)_{\\min} > 0$（严格大于），则证毕

> 若等号在区间内某点成立（如 $F(x) \\ge 0$），通常使用 $F(x)_{\\min} = 0$ 的论证方式。

### 二、常见需记忆的不等式

以下不等式在解题中可直接使用（但答题时最好简要证明）：

**指数类**：
- $e^x \\ge x + 1$（$x \\in \\mathbb{R}$），等号仅当 $x = 0$
- $e^x \\ge e \\cdot x$（$x \\in \\mathbb{R}$），等号仅当 $x = 1$
- $e^x > 1 + x + \\frac{x^2}{2}$（$x > 0$）

**对数类**：
- $\\ln(1+x) \\le x$（$x > -1$），等号仅当 $x = 0$
- $\\frac{x}{1+x} \\le \\ln(1+x) \\le x$（$x > 0$）
- $\\ln x \\le x - 1$（$x > 0$），等号仅当 $x = 1$

### 三、多阶求导策略

当 $F'(x)$ 的符号不能直接判断时，继续求导：

1. 求 $F''(x)$，分析 $F'(x)$ 的单调性
2. 若 $F''(x) > 0$（或 $< 0$），则 $F'(x)$ 单调，结合 $F'($某点$) = 0$ 可得其符号
3. 由 $F'(x)$ 的符号再分析 $F(x)$ 的单调性和最小值

**关键**：找到一阶或二阶导数的零点，通常是 $x = 0$ 或 $x = 1$ 等特殊点。

### 四、构造函数的灵活技巧

**技巧 1：适当放缩**

有时直接构造 $F(x) = f(x) - g(x)$ 过于复杂，可先利用已知不等式放缩：

> 证 $e^x + \\ln(1+x) > 1$（$x > 0$）：
> 由 $e^x > 1 + x$ 和 $\\ln(1+x) > 0$（$x > 0$），得 $e^x + \\ln(1+x) > 1 + x + 0 > 1$。

**技巧 2：双边不等式**

证 $A(x) < B(x) < C(x)$ 型双边不等式，通常分别构造函数证明左半和右半。

**技巧 3：数列求和型不等式**

形如 $\\sum_{k=1}^{n} a_k < S$ 的不等式，通常思路是：
- 找到一个函数不等式（如 $\\ln x \\le x - 1$）
- 对 $x$ 依次取 $k$（或 $\\frac{k}{n}$ 等），得到 $n$ 个不等式
- 累加（或累乘）得到最终结论

### 五、不等式的参数证明

当不等式中含参数时，需找到参数的临界值。常用方法：

1. **端点效应**：不等式在等号成立处的导数条件确定参数临界值
2. **分离参数**：转化为 $a \\le h(x)$（或 $a \\ge h(x)$），求 $h(x)$ 的最值
3. **不等式逼近**：利用已知不等式（如 $e^x \\ge x+1$）放缩，找到参数的充分条件
`;

export default lecture6_3;
