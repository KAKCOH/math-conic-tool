// 6.3 导数与不等式证明 讲义
const lecture6_3 = `
## 导数与不等式证明

### 一、构造函数法——标准范式

证明不等式 $f(x) > g(x)$（$x \\in I$）的标准范式：

1. 构造 $F(x) = f(x) - g(x)$
2. 对 $F(x)$ 求导，分析单调性和最值
3. 若有 $F(x)_{\\min} > 0$（严格大于），则证毕

> 若等号在区间内某点成立（如 $F(x) \\ge 0$），通常使用 $F(x)_{\\min} = 0$ 的论证方式。

### 二、常见需记忆的不等式

以下不等式在解题中可直接使用（但答题时最好简要证明）：

**指数类**：
- $e^x \\ge x + 1$（$x \\in \\mathbb{R}$），等号仅当 $x = 0$
- $e^x \\ge e \\cdot x$（$x \\in \\mathbb{R}$），等号仅当 $x = 1$
- $e^x > 1 + x + \\frac{x^2}{2}$（$x > 0$）

**对数类**：
- $\\ln(1+x) \\le x$（$x > -1$），等号仅当 $x = 0$
- $\\frac{x}{1+x} \\le \\ln(1+x) \\le x$（$x > 0$）
- $\\ln x \\le x - 1$（$x > 0$），等号仅当 $x = 1$

### 三、多阶求导策略

当 $F'(x)$ 的符号不能直接判断时，继续求导：

1. 求 $F''(x)$，分析 $F'(x)$ 的单调性
2. 若 $F''(x) > 0$（或 $< 0$），则 $F'(x)$ 单调，结合 $F'($某点$) = 0$ 可得其符号
3. 由 $F'(x)$ 的符号再分析 $F(x)$ 的单调性和最小值

**关键**：找到一阶或二阶导数的零点，通常是 $x = 0$ 或 $x = 1$ 等特殊点。

### 四、放缩法体系

放缩是导数压轴题最高频的技巧。核心思想：用一个**更容易处理的函数**来「上下夹住」目标函数。

#### 4.1 切线放缩族（基础）

所有放缩的根基是**切线放缩**——利用凸函数图像在切线上方（或凹函数在切线下方）这一几何事实。

**指数族**（$f(x) = e^x$ 是凸函数，图像在任意切线之上）：

在 $x = 0$ 处展开：$e^x \\ge x + 1$（等号 $x = 0$）

在 $x = 1$ 处展开：$e^x \\ge ex$（等号 $x = 1$）

代换变形：用 $x-1$ 替换 $x$，得 $e^{x-1} \\ge x$，即 $e^x \\ge ex$

一般形式：$e^x \\ge \\frac{e^k}{k!}x^k$ 不精确但有用（$e^x \\ge 1 + x + \\frac{x^2}{2}$，$x > 0$ 时）

**对数族**（$f(x) = \\ln x$ 是凹函数，图像在任意切线之下）：

在 $x = 1$ 处展开：$\\ln x \\le x - 1$（等号 $x = 1$）

代换 $x \\to 1+x$：$\\ln(1+x) \\le x$（$x > -1$）

代换 $x \\to \\frac{1}{x}$：$\\ln x \\ge 1 - \\frac{1}{x}$（$x > 0$）

夹逼形式：$\\frac{x-1}{x} \\le \\ln x \\le x-1$（$x > 0$）

#### 4.2 进阶放缩：飘带放缩

对 $\\ln x$ 的更精确估计——通过积分 $\\frac{1}{t}$ 得到的有理分式上下界：

$$\\frac{2(x-1)}{x+1} \\le \\ln x \\le \\frac{x^2 - 1}{2x} = \\frac{1}{2}\\left(x - \\frac{1}{x}\\right) \\quad (x \\ge 1)$$

（当 $0 < x \\le 1$ 时不等号反向）

飘带放缩的精度**高于**切线放缩（因为用了二次逼近），适合切线放缩「过度」导致结论太弱的场景。

#### 4.3 放缩的「度」——何时用哪种

| 放缩 | 精度 | 适用场景 | 注意 |
|------|------|---------|------|
| $e^x \\ge x+1$ | 一阶 | 证 $e^x$ 相关的简单下界 | 最基础，用错方向最常见 |
| $e^x \\ge ex$ | 一阶（最优切线） | $e^x$ 与 $x$ 比较 | 等号在 $x=1$，与 $x+1$ 互为补充 |
| $\\ln x \\le x-1$ | 一阶 | 证 $\\ln x$ 的简单上界 | 代入 $x \\to \\frac{k}{n}$ 即得数列放缩 |
| $\\frac{x-1}{x} \\le \\ln x \\le x-1$ | 一阶夹逼 | 需要同时上下界 | 两个方向结合常能精准卡位 |
| 飘带放缩 | 二阶 | 切线放缩精度不够时 | 有线性的「太弱」风险，飘带有理式更高精度 |
| ALG 不等式 | 精确 | 含 $\\frac{a-b}{\\ln a - \\ln b}$ 的表达式 | 直接套用，注意 $a,b > 0$ 且 $a \\neq b$ |

### 五、数列求和型不等式

导数和数列的结合是近年高考压轴的高频题型。典型模式：

> 先证一个**函数不等式** → 对自变量**赋值**得到数列项不等式 → **累加/累乘**得数列不等式

#### 标准操作流

**Step 1：用导数证明函数不等式**

如证 $\\ln(1+x) \\le x$（$x > -1$）：构造 $F(x) = \\ln(1+x) - x$，$F'(x) = \\frac{1}{1+x} - 1 = -\\frac{x}{1+x}$，$F(0) = 0$，得 $F(x) \\le 0$。

**Step 2：赋值**

令 $x = \\frac{1}{k}$，得 $\\ln\\left(1 + \\frac{1}{k}\\right) \\le \\frac{1}{k}$，即 $\\ln(k+1) - \\ln k \\le \\frac{1}{k}$。

**Step 3：累加（裂项相消）**

对 $k = 1, 2, \\ldots, n$ 累加：

$$\\sum_{k=1}^n [\\ln(k+1) - \\ln k] \\le \\sum_{k=1}^n \\frac{1}{k}$$

左边相消得 $\\ln(n+1) - \\ln 1 = \\ln(n+1)$，故 $\\ln(n+1) \\le 1 + \\frac{1}{2} + \\cdots + \\frac{1}{n}$。

**Step 4（可选）：调放缩方向**

若嫌累加后下界太弱，可调整赋值点或换更精确的放缩式。

#### 典型例题

**例 1·累加型**：证明 $\\sum_{k=1}^{n} \\frac{1}{k} > \\ln(n+1)$。

> 用 $\\frac{1}{k} > \\ln(1 + \\frac{1}{k}) = \\ln(k+1) - \\ln k$，累加即证。

**例 2·累乘型**：证明 $\\prod_{k=1}^{n} (1 + \\frac{1}{k}) \\le n+1$。

> 取对数：$\\sum \\ln(1 + \\frac{1}{k}) \\le \\ln(n+1)$。由 $\\ln(1+x) \\le x$，取 $x = \\frac{1}{k}$，得 $\\sum \\frac{1}{k}$。但此方向错误（调和级数发散）。应直接计算 $(1+\\frac{1}{1})(1+\\frac{1}{2})\\cdots(1+\\frac{1}{n}) = \\frac{2}{1} \\cdot \\frac{3}{2} \\cdots \\frac{n+1}{n} = n+1$。

#### 防坑要点

- 放缩前先估算「太大」还是「太小」：累加型注意放缩方向不能反
- 前两项常需**单独处理**（不放缩），否则容易「放缩过度」
- 检验放缩结果时，取 $n=1,2$ 心算验证

### 六、不等式的参数证明

含参不等式的参数临界值寻找策略：

1. **端点效应**：等号成立处的导数条件锁定临界值（详见 5.4）
2. **分离参数**：$a \\le h(x)$ 恒成立 $\\iff a \\le h_{\\min}$，求 $h_{\\min}$ 时可能需要放缩辅助
3. **放缩逼近**：用已知不等式（$e^x \\ge x+1$ 等）消去指数/对数，将含参问题转化为多项式最值
`;

export default lecture6_3;
